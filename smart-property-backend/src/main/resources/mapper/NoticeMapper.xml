<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.smartproperty.mapper.NoticeMapper">

    <insert id="insert" parameterType="com.example.smartproperty.entity.Notice"
            useGeneratedKeys="true" keyProperty="id">
        insert into notice(title, content, publisher_id, publisher_name, pinned, created_at)
        values(#{title}, #{content}, #{publisherId}, #{publisherName}, #{pinned}, now())
    </insert>

    <select id="findAll" resultType="com.example.smartproperty.entity.Notice">
        select * from notice order by pinned desc, created_at desc, id desc
    </select>
</mapper>
