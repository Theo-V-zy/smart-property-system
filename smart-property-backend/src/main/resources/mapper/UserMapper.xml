<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.smartproperty.mapper.UserMapper">

    <insert id="insert" parameterType="com.example.smartproperty.entity.User"
            useGeneratedKeys="true" keyProperty="id">
        insert into sys_user(role, username, password_hash, name, phone, community_name, address, business_scope, avatar, created_at, updated_at)
        values(#{role}, #{username}, #{passwordHash}, #{name}, #{phone}, #{communityName}, #{address}, #{businessScope}, #{avatar}, now(), now())
    </insert>

    <select id="findByUsernameAndRole" resultType="com.example.smartproperty.entity.User">
        select * from sys_user where username = #{username} and role = #{role} limit 1
    </select>

    <select id="findById" resultType="com.example.smartproperty.entity.User">
        select * from sys_user where id = #{id}
    </select>

    <update id="updateProfile" parameterType="com.example.smartproperty.entity.User">
        update sys_user
        set name = #{name},
            phone = #{phone},
            address = #{address},
            business_scope = #{businessScope},
            avatar = #{avatar},
            updated_at = now()
        where id = #{id}
    </update>

    <update id="updatePassword" parameterType="com.example.smartproperty.entity.User">
        update sys_user set password_hash = #{passwordHash}, updated_at = now() where id = #{id}
    </update>
</mapper>
